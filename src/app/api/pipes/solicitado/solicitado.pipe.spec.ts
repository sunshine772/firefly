import { SolicitadoPipe } from './solicitado.pipe';

describe('SolicitadoPipe', () => {
  it('create an instance', () => {
    const pipe = new SolicitadoPipe();
    expect(pipe).toBeTruthy();
  });
});
